# Task
Produce `demo-arpeggios.mp4` — a ~40-70s landscape (1440x900) Remotion demo video of the ready-made **Arpeggios** drill (`/tools/arpeggios`) — end-to-end through the established pipeline, then publish it to both distribution spots. Same shape as the shipped `demo-chord-drill.mp4` phase, tool=arpeggios.

# Context
Piano Suite's landing page embeds a finished product demo (`demo-web2.mp4`) and each ready-made drill gets a sibling demo video; `demo-chord-drill.mp4` shipped in the previous phase (commit 82f7899). This phase makes the second one, for Arpeggios: a two-phase blocked-practice drill on 7-note minor-11 arpeggio cells — a root/hold phase (left-hand root against each arpeggio cell) followed by a sequence phase, with a countdown, target-note prompt, cell strip, next/restart controls, miss filters, and stats. Anki is NOT running and Arpeggios has no Anki integration; do not build beats that require it. The pipeline, its hard rules, and its commands are documented in `/home/justin/.agents/skills/piano-demo-video/SKILL.md` — follow it exactly; do not invent a new composition or framework.

# Relevant files
- `/home/justin/.agents/skills/piano-demo-video/SKILL.md` — the pipeline contract: numeric-only verification, capture rules, beat cutting, TTS, QA, publish. Your primary reference.
- `/home/justin/piano-suite/.paseo-delegate/capture-web2.mjs` and `.paseo-delegate/capture-chord-drill.mjs` — reference capture scripts (Clerk setup, MOCK_MIDI injection, onboarding flag, per-shot contexts). Model `capture-arpeggios.mjs` on them; do not modify either reference.
- `/home/justin/piano-suite/app/tools/arpeggios/page.tsx` and `/home/justin/piano-suite/components/drills/arpeggios/arpeggios.tsx` — the tool you are filming. Read it first. Key testids: `arpeggios-drill`, `arpeggio-chord-name`, `arpeggio-target-note`, `arpeggio-cell-strip`, `arpeggio-lh-notes`, `customize-sequence-btn`, `next-chord-btn`, `restart-chord-btn`, `countdown-seconds-input`.
- `/home/justin/piano-content/src/PianoVideo.tsx` — the Remotion composition; `/home/justin/piano-content/src/config.generated.ts` — generated by `tts.mjs` (shared slot — see Chain handoff).
- `/home/justin/piano-content/script-web.txt` + `scenes-web3.json`, `script-chord-drill.txt` + `scenes-chord-drill.json` — study their shape (one idea per beat); NEVER edit or overwrite them.
- `/home/justin/piano-suite/lib/local-practice-history.ts` — arpeggios tracking keys: `blocked-drill-arpeggio-log` (+ `blocked-drill-arpeggio-miss-log`). Seed plausible multi-day data (grades Again|Hard|Good|Easy) before the live run if a beat shows the tracking/stats panel; seed ONLY these tracking keys.
- `/home/justin/piano-suite/.paseo-delegate/demo-videos/plan.json` — this phase's acceptance criteria.

# Output format
- `/home/justin/piano-content/out/demo-arpeggios.mp4` (the render), copied to `/home/justin/piano-content/dist/demo-arpeggios.mp4` and `/home/justin/piano-suite/public/demo-arpeggios.mp4`.
- `/home/justin/piano-suite/.paseo-delegate/capture-arpeggios.mjs` — your capture script, committed to the piano-suite repo.
- `/home/justin/piano-content/script-arpeggios.txt` + `/home/justin/piano-content/scenes-arpeggios.json` — your script (one idea per beat) and beat mapping (new files; never clobber prior phases' files).
- A beat table in your completion summary: time, spoken gist, on-screen action per beat, plus the `config.generated.ts` md5 you rendered against.
- All verification is numeric: `ffmpeg signalstats` (YAVG/SATAVG/YDIF) on downscaled/cropped regions, PSNR vs reference PNGs, and tesseract OCR (`export TESSDATA_PREFIX=$(dirname $(find / -name eng.traineddata 2>/dev/null | head -1))`, frames dumped to `/tmp/opencode/*.png`). NEVER read images yourself.

# Chain handoff (from the video-chord-drill verdict — treat as binding)
- **config.generated.ts is an adversarial shared slot.** A concurrent vertical-cut process clobbered it mid-render once before. Protocol: regenerate from your own script/scenes IMMEDIATELY before bundling/rendering (`node scripts/tts.mjs script-arpeggios.txt --scenes scenes-arpeggios.json --web --tts-speed 0.9`, Kokoro `af_heart`); snapshot the slot to `/tmp/opencode/` with md5 before and after regeneration; re-verify the md5 right before `npx remotion render`; record the rendered-against config md5 in your completion summary.
- **Leave comfortable fit margins.** On the chord-drill render one beat fit its TTS segments by only 7ms — legal but too thin to re-verify confidently. After reading segment frames from `config.generated.ts`, re-cut any beat shorter than its segment total plus real headroom.
- **Environment:** local Convex (:3210) is dead; settings/history live in Convex cloud (intent-snail-943). Configure the drill through the UI (testids above) — do not try to seed settings. Shared :3001 died mid-phase and :3000 is the opencode proxy — start your own dev server on port >=3002 (`BASE_URL=http://localhost:3002`, `set -a && . ./.env.local && set +a`). Expect cold-compile dead time on a fresh server; one browser context per capture run; MOCK_MIDI injected so the drill receives notes.
- **tesseract here lacks tsv/hocr configs** — use full-frame psm 11 + band crops for all OCR.
- **Render with `--concurrency=2`** if the machine is loaded.
- `tailscale serve status` — if a :8454 route serving `~/piano-content/dist` exists, `curl` the published file URL and expect 200; if the route does not exist, report it in TOOLING NOTES (do not reconfigure tailscale serve).

# Tool and source guidance
- Pipeline order (from the skill): capture (Playwright, `.webm` -> mp4 via the given ffmpeg line) -> verify numerically -> cut beat windows -> script + scenes JSON -> TTS per above -> read segment frames from `config.generated.ts` and re-cut any beat shorter than its segment -> `npx remotion render PianoVideo out/demo-arpeggios.mp4` -> numeric QA per beat -> publish both spots.
- Never overwrite another variant's files (`script-web*`, `scenes-web*`, `out/demo-web2.mp4`, `public/demo-web2.mp4`, `demo-chord-drill*` in out/dist/public, `script-chord-drill.txt`, `scenes-chord-drill.json`, `/tmp/opencode/cfg-vert-backup.ts`). New files only, named exactly as in Output format.
- Worktree/commit discipline: commit only `.paseo-delegate/capture-arpeggios.mjs`, `.paseo-delegate/demo-videos/` contract files, and `public/demo-arpeggios.mp4` to the piano-suite repo, scoped paths, provenance trailers (Phase: video-arpeggios; Agent-Id: `echo $PASEO_AGENT_ID`; Session-Id from `~/.paseo/agents/home-justin-piano-suite/$PASEO_AGENT_ID.json`). `~/piano-content` is not a git repo — files only there. If `git index.lock` conflicts with another process, wait and retry; never `rm -rf .git`.
- Write incrementally. Do not pre-plan the whole artefact.

# Task boundaries
- Do NOT modify any app source under `/home/justin/piano-suite/app`, `components/`, `lib/` (capture scripts and contract files are your only piano-suite writes besides the published mp4).
- Do NOT touch `capture-web2.mjs` / `capture-chord-drill.mjs` as references to keep intact; write your own file.
- Do NOT reconfigure tailscale serve, the Convex backend, or anyone else's dev servers.

# Effort budget
2 hours wall clock. Expect 5-8 beats. If capture or render proves impossible in that budget, stop with STATUS: blocked, what you tried, and numeric evidence.

# Acceptance criteria
- [ ] `ffprobe /home/justin/piano-content/out/demo-arpeggios.mp4` reports h264, 1440x900, duration >= 24s — validator re-runs it.
- [ ] `~/piano-content/dist/demo-arpeggios.mp4` and `/home/justin/piano-suite/public/demo-arpeggios.mp4` exist and match the render (validator compares sizes/hashes).
- [ ] `/home/justin/piano-suite/.paseo-delegate/capture-arpeggios.mjs` exists and is committed with provenance trailers — validator checks `git log --format=full` on that path.
- [ ] `/home/justin/piano-content/script-arpeggios.txt` and `scenes-arpeggios.json` exist; script text reads one idea per beat — validator reads the files.
- [ ] The summary carries a beat table; for each beat the validator independently OCRs the segment midpoint frame and finds the expected on-screen UI text AND caption, plus the config md5 to re-derive fit.
- [ ] No frozen beats: validator runs signalstats YDIF checks on downscaled frames at beat midpoints.
- [ ] `config.generated.ts` segments all fit their beat windows with real headroom (worker attests the re-cut check; validator re-derives from `config.generated.ts` + beat table durations using the recorded md5).
- [ ] Piano-suite tree: nothing NEW dirty beyond the pre-existing baseline (`docs/quick-fixes-2026-09/`, `public/demo-web.mp4`; `demo-web2.mp4`, `demo-chord-drill.mp4`, and contract files are tracked — commit your own additions).

# Constraints
- Numeric verification only — reading screenshots/video frames as images is forbidden and invalidates the phase.
- The demo copy should speak to self-taught beginners; plain, welcoming, no hype.
- Distinct first line: never re-render or modify the shipped demo-web2 or demo-chord-drill cuts.

# Completion contract
Your final chat message must be your completion summary:
STATUS: complete|blocked|failed
SUMMARY: ...
FILES CHANGED: ...
VERIFICATION: ... (ffmpeg/OCR/ffprobe outputs, beat table, config md5)
BLOCKERS: ...
TOOLING NOTES: ...
HANDOFF NOTES: ...
