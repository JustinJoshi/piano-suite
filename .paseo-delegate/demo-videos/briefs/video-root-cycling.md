# Task
Produce `demo-root-cycling.mp4` — a ~40-70s landscape (1440x900) Remotion demo video of the ready-made **Root Cycling** drill (`/tools/root-cycling`) — end-to-end through the established pipeline, then publish it to both distribution spots. Same shape as the shipped `demo-chord-drill.mp4` and `demo-arpeggios.mp4` phases, tool=root-cycling.

# Context
Piano Suite's landing page embeds the product demo (`demo-web2.mp4`) and each ready-made drill gets a sibling demo video; `demo-chord-drill.mp4` (commit 82f7899) and `demo-arpeggios.mp4` (commit c5d4050) already shipped. This phase makes the third one, for Root Cycling: one fixed chord/arpeggio idea drilled across random roots in all 12 keys — a mode toggle (chord mode drills a single quality as a block chord in a random key each rep; arpeggio mode transposes the canonical minor-11th shape), a quality picker, root set customization, a prompt symbol + cell strip, start/stop/skip/next controls, per-rep history on the card, and a Tracking panel. Anki is NOT running and Root Cycling has no Anki integration; do not build beats that require it. The pipeline, its hard rules, and its commands are documented in `/home/justin/.agents/skills/piano-demo-video/SKILL.md` — follow it exactly; do not invent a new composition or framework.

# Relevant files
- `/home/justin/.agents/skills/piano-demo-video/SKILL.md` — the pipeline contract: numeric-only verification, capture rules, beat cutting, TTS, QA, publish. Your primary reference.
- `/home/justin/piano-suite/.paseo-delegate/capture-chord-drill.mjs` and `.paseo-delegate/capture-arpeggios.mjs` — reference capture scripts (Clerk setup, MOCK_MIDI injection, onboarding flag, per-shot contexts). Model `capture-root-cycling.mjs` on them; do not modify either reference.
- `/home/justin/piano-suite/app/tools/root-cycling/page.tsx` and `/home/justin/piano-suite/components/drills/root-cycling/root-cycling.tsx` — the tool you are filming. Read them first. Key testids: `root-cycling-drill`, `rc-mode-chord` / `rc-mode-arpeggio`, `rc-quality-<suffix>`, `rc-customize-btn`, `rc-root-<name>`, `rc-reset-roots-btn`, `rc-prompt-symbol`, `rc-cell-strip`, `rc-start-btn`, `rc-stop-btn`, `rc-skip-btn`, `rc-next-btn`, `rc-history`, `rc-clear-roots-btn`.
- `/home/justin/piano-suite/components/tracking/root-cycling-panel.tsx` — the Tracking panel the final beats show.
- `/home/justin/piano-content/src/PianoVideo.tsx` — the Remotion composition; `/home/justin/piano-content/src/config.generated.ts` — generated by `tts.mjs` (shared slot — see Chain handoff).
- `/home/justin/piano-suite/lib/local-practice-history.ts` — the root-cycling tracking key: `blocked-drill-rootcycle-log`. Seed plausible multi-day data (grades Again|Hard|Good|Easy) before the live run if a beat shows the tracking/stats panel; seed ONLY this tracking key.
- `/home/justin/piano-suite/.paseo-delegate/demo-videos/plan.json` — this phase's acceptance criteria.

# Output format
- `/home/justin/piano-content/out/demo-root-cycling.mp4` (the render), copied to `/home/justin/piano-content/dist/demo-root-cycling.mp4` and `/home/justin/piano-suite/public/demo-root-cycling.mp4`.
- `/home/justin/piano-suite/.paseo-delegate/capture-root-cycling.mjs` — your capture script, committed to the piano-suite repo.
- `/home/justin/piano-content/script-root-cycling.txt` + `/home/justin/piano-content/scenes-root-cycling.json` — your script (one idea per beat) and beat mapping (new files; never clobber prior phases' files).
- `/home/justin/piano-suite/.paseo-delegate/demo-videos/summaries/video-root-cycling.json` — your completion summary as a single JSON object matching `/home/justin/piano-suite/.paseo-delegate/demo-videos/schemas/worker-summary.json`; embed the full beat table in `verification`.
- A beat table in both the JSON summary and your final chat message: time window, spoken gist, on-screen action, and expected OCR strings per beat, plus the `config.generated.ts` md5 you rendered against.
- All verification is numeric: `ffmpeg signalstats` (YAVG/SATAVG/YDIF) on downscaled/cropped regions, PSNR vs reference PNGs, and tesseract OCR (`export TESSDATA_PREFIX=$(dirname $(find / -name eng.traineddata 2>/dev/null | head -1))`, frames dumped to `/tmp/opencode/*.png`). NEVER read images yourself.

# Tool and source guidance
- Pipeline order (from the skill): capture (Playwright, `.webm` -> mp4 via the given ffmpeg line) -> verify numerically -> cut beat windows -> script + scenes JSON -> TTS per the Chain handoff -> read segment frames from `config.generated.ts` and re-cut any beat shorter than its segment total plus headroom -> `npx remotion render PianoVideo out/demo-root-cycling.mp4` -> numeric QA per beat -> publish both spots.
- Capture preconditions: `:3001` is dead and `:3000` is the opencode proxy — start your own dev server on port >= 3002 (`BASE_URL=http://localhost:3002`, `set -a && . ./.env.local && set +a` from `/home/justin/piano-suite`). Expect cold-compile dead time on a fresh server; re-capture after warm-up. One browser context per capture run; seed demo data before the live run; MOCK_MIDI injected so the drill receives notes. Local Convex (:3210) is dead — settings/history live in Convex cloud (intent-snail-943); configure the drill through the UI (testids above), never by seeding settings. Clean up only your own server (pkill by port, never a broad `pkill -f "next dev"`).
- Never overwrite another variant's files (`script-web*`, `scenes-web*`, `scenes-vert*`, `script.txt`, `scenes.json`, `script-chord-drill.txt`, `scenes-chord-drill.json`, `script-arpeggios.txt`, `scenes-arpeggios.json`, any `demo-web2*` / `demo-chord-drill*` / `demo-arpeggios*` in out/dist/public, `/tmp/opencode/cfg-vert-backup.ts`). New files only, named exactly as in Output format.
- Worktree/commit discipline: commit only `.paseo-delegate/capture-root-cycling.mjs`, `.paseo-delegate/demo-videos/` contract files, and `public/demo-root-cycling.mp4` to the piano-suite repo, scoped paths, provenance trailers (Phase: video-root-cycling; Agent-Id: `echo $PASEO_AGENT_ID`; Session-Id from `~/.paseo/agents/home-justin-piano-suite/$PASEO_AGENT_ID.json`). `~/piano-content` is not a git repo — files only there. If `git index.lock` conflicts with another process, wait and retry; never `rm -rf .git`.
- `tailscale serve status` — if a :8454 route serving `~/piano-content/dist` exists, `curl` the published file URL and expect 200; if the route does not exist, report it in TOOLING NOTES (do not reconfigure tailscale serve).
- Write incrementally. Do not pre-plan the whole artefact.

# Chain handoff (from the video-arpeggios verdict — treat as binding)
- **config.generated.ts is an adversarial shared slot.** A concurrent process clobbered it mid-render once (video-chord-drill). Protocol: snapshot the slot to `/tmp/opencode/` with md5 before regeneration; regenerate from your own script/scenes IMMEDIATELY before bundling/rendering (`node scripts/tts.mjs script-root-cycling.txt --scenes scenes-root-cycling.json --web --tts-speed 0.9`, Kokoro `af_heart`); snapshot the regenerated file with md5; count sceneSegments after every regeneration (a stale-scene generation can silently drop segments); re-verify the md5 right before `npx remotion render`; record the rendered-against config md5 in your summary.
- **Leave comfortable fit margins.** After reading segment frames from `config.generated.ts`, re-cut any beat shorter than its segment total plus >= 2s headroom (the chord-drill render once fit by 7ms — legal but too thin to re-verify).
- **tesseract here lacks tsv/hocr configs** — use full-frame psm 11 + band crops for all OCR.
- **Render with `--concurrency=2`** if the machine is loaded.
- **Publish both spots with identical md5** across out/dist/public.

# Task boundaries
- Do NOT modify any app source under `/home/justin/piano-suite/app`, `components/`, `lib/` (capture scripts and contract files are your only piano-suite writes besides the published mp4).
- Do NOT touch `capture-chord-drill.mjs` / `capture-arpeggios.mjs` as references to keep intact; write your own file.
- Do NOT reconfigure tailscale serve, the Convex backend, or anyone else's dev servers.

# Effort budget
2 hours wall clock. Expect 5-10 beats. If capture or render proves impossible in that budget, stop with STATUS: blocked, what you tried, and numeric evidence.

# Acceptance criteria
- [ ] `ffprobe /home/justin/piano-content/out/demo-root-cycling.mp4` reports h264, 1440x900, duration >= 24s — validator re-runs it.
- [ ] `~/piano-content/dist/demo-root-cycling.mp4` and `/home/justin/piano-suite/public/demo-root-cycling.mp4` exist and match the render (validator compares sizes/hashes).
- [ ] `/home/justin/piano-suite/.paseo-delegate/capture-root-cycling.mjs` exists and is committed with provenance trailers — validator checks `git log --format=full` on that path.
- [ ] `/home/justin/piano-content/script-root-cycling.txt` and `scenes-root-cycling.json` exist; script text reads one idea per beat — validator reads the files. Prior phases' script/scenes files unchanged.
- [ ] The JSON summary at `.paseo-delegate/demo-videos/summaries/video-root-cycling.json` matches `schemas/worker-summary.json` and carries the full beat table (time window, spoken gist, on-screen action, expected OCR per beat) plus the rendered-against config md5; for each beat the validator independently OCRs the segment midpoint frame and finds the expected on-screen UI text AND caption.
- [ ] No frozen beats: validator runs signalstats YDIF checks on downscaled frames per segment window.
- [ ] `config.generated.ts` segments all fit their beat windows with >= 2s headroom; validator re-derives fit from `config.generated.ts` + clip ffprobe durations using the recorded config md5, and cross-checks the before/after slot snapshots.
- [ ] Piano-suite tree: nothing NEW dirty beyond the pre-existing baseline (`docs/quick-fixes-2026-09/`, `public/demo-web.mp4`; `demo-web2.mp4`, `demo-chord-drill.mp4`, `demo-arpeggios.mp4`, capture scripts, and contract files are tracked — commit your own additions).

# Constraints
- Numeric verification only — reading screenshots/video frames as images is forbidden and invalidates the phase.
- The demo copy should speak to self-taught beginners; plain, welcoming, no hype.
- Distinct first line: never re-render or modify the shipped demo-web2, demo-chord-drill, or demo-arpeggios cuts.

# Completion contract
Your final chat message must be your completion summary:
STATUS: complete|blocked|failed
SUMMARY: ...
FILES CHANGED: ...
VERIFICATION: ... (ffmpeg/OCR/ffprobe outputs, full beat table, config md5)
BLOCKERS: ...
TOOLING NOTES: ... (defects/surprises in tools — dev server, gates, capture stack; `none` if none)
HANDOFF NOTES: ...
