# Task
Produce `demo-chord-drill.mp4` — a ~40-70s landscape (1440x900) Remotion demo video of the ready-made **Chord Drill** (`/tools/chord-drill`) — end-to-end through the established pipeline, then publish it to both distribution spots.

# Context
Piano Suite's landing page now embeds a finished product demo (`demo-web2.mp4`). The app has four ready-made drills plus the Workshop, each getting its own sibling demo video. This phase makes the first one, for Chord Drill: a blocked-practice chord drill with a timer, stats, and optional AnkiConnect integration (Anki is NOT running — every Anki feature must degrade gracefully; do not build beats that require it). The pipeline, its hard rules, and its commands are documented in `/home/justin/.agents/skills/piano-demo-video/SKILL.md` — follow it exactly; do not invent a new composition or framework.

# Relevant files
- `/home/justin/.agents/skills/piano-demo-video/SKILL.md` — the pipeline contract: numeric-only verification, capture rules, beat cutting, TTS, QA, publish. Your primary reference.
- `/home/justin/piano-suite/.paseo-delegate/capture-web2.mjs` — reference capture script (Clerk setup, MOCK_MIDI injection, onboarding flag, per-shot contexts). Model `capture-chord-drill.mjs` on it.
- `/home/justin/piano-suite/app/tools/chord-drill/page.tsx` and `/home/justin/piano-suite/components/drills/chord-drill/` — the tool you are filming. Read it; know what the drill shows (target chord prompt, timer, grade flow, stats).
- `/home/justin/piano-content/src/PianoVideo.tsx` — the Remotion composition; `/home/justin/piano-content/src/config.generated.ts` — generated by `tts.mjs`.
- `/home/justin/piano-content/script-web.txt` + `scenes-web3.json` — the shipped web cut's script/scenes: study their shape (one idea per beat), do NOT edit or overwrite them.
- `/home/justin/piano-suite/lib/local-practice-history.ts` — the localStorage key (`blocked-drill-first-chord-log`) if a beat shows history/stats panels; seed plausible multi-day data (grades Again|Hard|Good|Easy) before the live run.
- `/home/justin/piano-suite/.paseo-delegate/demo-videos/plan.json` — this phase's acceptance criteria.

# Output format
- `/home/justin/piano-content/out/demo-chord-drill.mp4` (the render), copied to `/home/justin/piano-content/dist/demo-chord-drill.mp4` and `/home/justin/piano-suite/public/demo-chord-drill.mp4`.
- `/home/justin/piano-suite/.paseo-delegate/capture-chord-drill.mjs` — your capture script, committed to the piano-suite repo.
- `/home/justin/piano-content/script-chord-drill.txt` + `/home/justin/piano-content/scenes-chord-drill.json` — your script (one idea per beat) and beat mapping (new files; never clobber the web-cut originals).
- A beat table in your completion summary: time, spoken gist, on-screen action per beat.
- All verification is numeric: `ffmpeg signalstats` (YAVG/SATAVG/YDIF) on downscaled/cropped regions, PSNR vs reference PNGs, and tesseract OCR (`export TESSDATA_PREFIX=$(dirname $(find / -name eng.traineddata 2>/dev/null | head -1))`, frames dumped to `/tmp/opencode/*.png`). NEVER read images yourself.

# Tool and source guidance
- Pipeline order (from the skill): capture (Playwright, `.webm` -> mp4 via the given ffmpeg line) -> verify numerically -> cut beat windows -> script + scenes JSON -> `node scripts/tts.mjs script-chord-drill.txt --scenes scenes-chord-drill.json --web --tts-speed 0.9` (Kokoro `af_heart`) -> read segment frames from `config.generated.ts` and re-cut any beat shorter than its segment -> `npx remotion render PianoVideo out/demo-chord-drill.mp4` -> numeric QA per beat -> publish both spots.
- Capture preconditions: dev servers already run on :3000 and :3001 — but verify the app actually loads there (`curl` + OCR of your captured frames to confirm you filmed the right tool's UI); if pages error (e.g. Convex down), stop with STATUS: blocked and evidence. Use `BASE_URL=http://localhost:3001` and the `set -a && . ./.env.local && set +a` env pattern from the skill. One browser context per capture run; seed demo data before the live run; MOCK_MIDI injected so the drill receives notes.
- Never overwrite another variant's files (`script-web*`, `scenes-web*`, `out/demo-web2.mp4`, `public/demo-web2.mp4`). New files only, named exactly as in Output format.
- Worktree/commit discipline: commit only `.paseo-delegate/capture-chord-drill.mjs`, `.paseo-delegate/demo-videos/` contract files, and `public/demo-chord-drill.mp4` to the piano-suite repo, scoped paths, provenance trailers (Phase: video-chord-drill; Agent-Id: `echo $PASEO_AGENT_ID`; Session-Id from `~/.paseo/agents/home-justin-piano-suite/$PASEO_AGENT_ID.json`). `~/piano-content` is not a git repo — files only there. If `git index.lock` conflicts with another process, wait and retry; never `rm -rf .git`.
- `tailscale serve status` — if a :8454 route serving `~/piano-content/dist` exists, `curl` the published file URL and expect 200; if the route does not exist, report it in TOOLING NOTES (do not reconfigure tailscale serve).
- Write incrementally. Do not pre-plan the whole artefact.

# Task boundaries
- Do NOT modify any app source under `/home/justin/piano-suite/app`, `components/`, `lib/` (capture scripts and contract files are your only piano-suite writes besides the published mp4).
- Do NOT touch `capture-web2.mjs` / other existing capture scripts as references to keep intact; write your own file.
- Do NOT reconfigure tailscale serve, the Convex backend, or the shared dev servers.

# Effort budget
2 hours wall clock. Expect 5-8 beats. If capture or render proves impossible in that budget, stop with STATUS: blocked, what you tried, and numeric evidence.

# Acceptance criteria
- [ ] `ffprobe /home/justin/piano-content/out/demo-chord-drill.mp4` reports h264, 1440x900, duration >= 24s — validator re-runs it.
- [ ] `~/piano-content/dist/demo-chord-drill.mp4` and `/home/justin/piano-suite/public/demo-chord-drill.mp4` exist and match the render (validator compares sizes/hashes).
- [ ] `/home/justin/piano-suite/.paseo-delegate/capture-chord-drill.mjs` exists and is committed with provenance trailers — validator checks `git log --format=full` on that path.
- [ ] `/home/justin/piano-content/script-chord-drill.txt` and `scenes-chord-drill.json` exist; script text reads one idea per beat — validator reads the files.
- [ ] The summary carries a beat table; for each beat the validator independently OCRs the segment midpoint frame and finds the expected on-screen UI text AND caption.
- [ ] No frozen beats: validator runs signalstats YDIF checks on downscaled frames at beat midpoints.
- [ ] `config.generated.ts` segments all fit their beat windows (worker attests re-cut check; validator re-derives from `config.generated.ts` + beat table durations).
- [ ] Piano-suite tree: nothing NEW dirty beyond the pre-existing baseline (`docs/quick-fixes-2026-09/`, `public/demo-web.mp4`, `public/demo-web2.mp4`, `public/demo-chord-drill.mp4` after your commit is part of the phase).

# Constraints
- Numeric verification only — reading screenshots/video frames as images is forbidden and invalidates the phase.
- The demo copy should speak to self-taught beginners; plain, welcoming, no hype.
- Distinct first line: never re-render or modify the shipped demo-web2 cut.

# Completion contract
Your final chat message must be your completion summary:
STATUS: complete|blocked|failed
SUMMARY: ...
FILES CHANGED: ...
VERIFICATION: ... (ffmpeg/OCR/ffprobe outputs, beat table)
BLOCKERS: ...
TOOLING NOTES: ...
HANDOFF NOTES: ...
