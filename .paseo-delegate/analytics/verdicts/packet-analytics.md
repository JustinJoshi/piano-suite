OUTCOME PACKET (phase analytics — final phase of this run)

## 1. Master plan's spec for phase N+1
None. `analytics` is the only phase in this run's plan (config `next:auto`,
planner never escalated, no phases remain). The packet is filed for the fleet
orchestrator and human review rather than routed to a next-brief author.

## 2. Worker's completion summary (raw)
STATUS: complete
SUMMARY: Added three analytics events (`door_clicked`, `page_created`, `block_added`) to `ANALYTICS_EVENTS` in `lib/analytics.ts`, each wired to a real call site. `door_clicked` fires from the `onClick` on each door `Link` in `components/welcome/door-chooser.tsx` (property: `doorId`, real ids are `play`/`build`/`learn`, not `explore` as the brief guessed). `page_created` (`origin: "scratch" | "fork"`) and `block_added` (`type`) fire from the pure store functions in `lib/custom-practice-storage.ts` — `createPracticePageInStore`, `forkPageIntoStore`, and `appendBlockToPage` — rather than from their callers, because the real UI call sites for page creation/forking/block-adding all live inside `app/**/page.tsx` files, where AGENTS.md explicitly bars `captureEvent`. Since Pro pages are authored through the same local store before `useWorkshopSync` debounces them to Convex, this single emit point covers both the Free and signed-in/Pro paths without touching `convex/workshop.ts`.
FILES CHANGED: `lib/analytics.ts`, `lib/custom-practice-storage.ts`, `components/welcome/door-chooser.tsx`, `AGENTS.md`, plus tests: `lib/__tests__/analytics.test.ts`, `lib/__tests__/custom-practice-storage.test.ts`, `components/welcome/__tests__/door-chooser.test.tsx`. Also tracked `.paseo-delegate/analytics/briefs/analytics.md` (force-added, matching the existing delegate-run provenance pattern).
VERIFICATION: `npm run lint` → 0 errors (12 pre-existing warnings, unrelated). `npm run test:unit:run` → 147 files / 1358 tests passed (≥1349 required). `npm run build` → exits 0 (had to remove a `node_modules` symlink I'd added for local test running — Turbopack rejects it as pointing outside the filesystem root; `next.config.ts` already has a `resolveProjectRoot()` fallback that finds the main repo's `node_modules` correctly once no worktree-local symlink shadows it, and `npx`/vitest resolve upward through parent directories on their own). `git diff` shows no `package.json`/`package-lock.json` changes. Branch pushed, PR #93 opened against `main`.
BLOCKERS: none.
TOOLING NOTES: A worktree with no local `node_modules` builds fine via the existing `next.config.ts` `resolveProjectRoot()` fallback and `npx`'s upward module resolution — do **not** symlink `node_modules` into a worktree to "fix" a missing binary; Turbopack treats a symlink pointing outside the project root as fatal (`Symlink [project]/node_modules is invalid, it points out of the filesystem root`). I hit this, diagnosed it, and removed the symlink before the final build.
HANDOFF NOTES: The door ids in this codebase are `play`/`build`/`learn` (the brief guessed `play`/`explore`/`learn` — `explore` is only used as `build`'s color hue in `door-chooser.tsx`'s `DOOR_TONES`, not as a door id). I used the real ids. For `page_created`/`block_added`, I deliberately deviated from the brief's literal wording ("wired from whatever component/hook actually calls them") by instrumenting the pure store functions in `lib/custom-practice-storage.ts` directly instead of their `app/**/page.tsx` callers, since AGENTS.md's page-component ban takes precedence and the store functions are the one place both Free and Pro flows funnel through — this is called out in both the commit message and the AGENTS.md edit for the next agent. I did not add a fourth event — no other call site met the "real, justified in one sentence" bar. `.paseo-delegate/analytics/verdicts/analytics.json` does not exist yet; per the brief that's presumably produced by a separate validator step, not the worker.

## 3. Validator's assessment (curated)
All acceptance criteria independently verified against the real diff and a from-scratch gate re-run. Emit sites: door_clicked at components/welcome/door-chooser.tsx:87,142; page_created at lib/custom-practice-storage.ts:260 (scratch) and :339 (fork); block_added at lib/custom-practice-storage.ts:292 — all outside lib/analytics.ts and outside __tests__, and the store functions have five real app call sites (components/custom-practice/practice-page-editor.tsx:98, components/custom-practice/command-palette.tsx:60, app/marketplace/page.tsx:95, app/marketplace/[id]/page.tsx:77, app/tools/workshop/blocks/page.tsx:35), so they are not test-only helpers. The store-function placement reasoning holds: two of those callers are app/**/page.tsx files where AGENTS.md bars captureEvent, and hooks/useWorkshopSync.ts:51 confirms Pro pages funnel through the same local store before the debounced upsertCustomDrill push, so one emit point covers Free and Pro. Free-path unit tests (lib/__tests__/custom-practice-storage.test.ts) exercise the store functions directly with no Convex/Clerk mocking; analytics.test.ts proves both no-op-without-key (posthog.capture never called, mirror still updates) and capture-with-key. Gate re-run: lint 0 errors (12 pre-existing warnings), 147 files / 1358 tests passed 0 failures (bar ≥1349), build exit 0. package.json/package-lock.json untouched. app/privacy untouched. Payloads carry only doorId, origin, type — no titles, free text, or email; consistent with lib/sentry.ts's PII scrubbing stance. All four commits carry complete Phase/Agent-Id/Session-Id/Brief/Verdict trailers with repo-root-relative paths; whole-repo tree clean; branch pushed; PR #93 open against main. Solid. Two notes for readers, neither a criterion: appendBlockToPage emits on every append, so remove-then-re-add counts again (fine for a North Star signal); the emit requires captureEvent to stay client-safe — true today since custom-practice-storage.ts is not imported by any Convex bundle, but any future server-side import of it would need re-checking. No existing test was weakened (event-list test still exact-locks the full set; storage and door-chooser changes are additive).

## 4. Validator's handoff_notes
Deltas vs the plan the next phase must absorb: (1) Door ids are play/build/learn, not play/explore/learn — lib/welcome-config.ts:64 types the union and explore appears only as the build door's color hue in door-chooser.tsx DOOR_TONES; correct any doc/plan referencing door ids. (2) New convention now documented in AGENTS.md: for actions whose real call site is inside app/**/page.tsx, emit analytics from the pure store function it calls instead (page_created/block_added in lib/custom-practice-storage.ts). (3) page_created origin values are scratch | fork; fork fires only when at least one block survives sanitization (negative test locks this). (4) No fourth event was added — the worker found no other call site meeting the brief's one-sentence-justification bar. (5) PR #93 (fleet/analytics → main) is already open; a fix-up push would update it, and the final PASS leaves landing it to the orchestrator. (6) Commit 89c0681 force-added the gitignored .paseo-delegate/analytics/briefs/analytics.md, matching the existing delegate-run tracking pattern; .gitignore itself untouched. Also note the tooling discovery for future worktree phases: never symlink node_modules into a worktree — Turbopack rejects an out-of-root symlink; rely on next.config.ts resolveProjectRoot() and npx upward resolution.

## 5. Tooling notes (verbatim)
A worktree with no local `node_modules` builds fine via the existing `next.config.ts` `resolveProjectRoot()` fallback and `npx`'s upward module resolution — do **not** symlink `node_modules` into a worktree to "fix" a missing binary; Turbopack treats a symlink pointing outside the project root as fatal (`Symlink [project]/node_modules is invalid, it points out of the filesystem root`). I hit this, diagnosed it, and removed the symlink before the final build.

## 6. Ledger extract
- 2026-09-16T20:08Z run init (mode:auto · workers:judge · next:auto · parallel:1 · fixups:2 · worker claude/claude-sonnet-5 · validator zai/glm-5.3-flash · wks_1eb10d7b1e2bd470)
- 2026-09-17T00:09Z spawn [Validator] analytics · b106cdc4 · zai/glm-5.3-flash · bypass_permissions
- 2026-09-17T00:10Z spawn [Worker] analytics · 015b522a · claude/claude-sonnet-5 · bypassPermissions · thinking high (sessionId corrected at 00:35Z: 7b3c270e)
- 2026-09-17T00:35Z worker_complete · status complete · PR #93 opened by the worker itself (process deviation, flagged, not penalized)
- 2026-09-17 (this session) orchestrator swap claude → zai/glm-acp-agent, operator-ordered; verdict collected from the still-live validator; PASS, drift detected, fixups 0 of 2; contract committed and branch pushed per run protocol
